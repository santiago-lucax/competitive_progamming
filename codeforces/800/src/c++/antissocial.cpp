#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using vi = vector<int>;

void solve() {
    int n;
    if (!(cin >> n)) return;
    
    vi chairs(n);
    for (int i = 0; i < n; i++) chairs[i] = i + 1;

    for (int i = 2; i < n; i ++) {
        
    }
}

int main() {
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 

    int t = 1;
    while (t--) {
        solve();
    }
    
    return 0;
}
